import nogamma, { gamma } from "./color.js";
import { rgb as rgb$1 } from "../../d3-color/src/color.js";
const rgb = function rgbGamma(y) {
  var color = gamma(y);
  function rgb2(start, end) {
    var r = color((start = rgb$1(start)).r, (end = rgb$1(end)).r), g = color(start.g, end.g), b = color(start.b, end.b), opacity = nogamma(start.opacity, end.opacity);
    return function(t) {
      start.r = r(t);
      start.g = g(t);
      start.b = b(t);
      start.opacity = opacity(t);
      return start + "";
    };
  }
  rgb2.gamma = rgbGamma;
  return rgb2;
}(1);
export {
  rgb as default
};
